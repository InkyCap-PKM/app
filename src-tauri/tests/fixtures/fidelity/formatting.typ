#import "/.inkycap/notebox.typ": *
#note(title: "Inline formatting")

= Inline formatting

This sentence has *bold*, _italic_, and `monospace` runs, plus a
#strike[struck phrase] and a #highlight[highlighted phrase]. A
#underline[underlined run] closes the set.

An em dash substitute, smart "quotes", and an accented résumé café
ensure UTF-8 text survives layout.
