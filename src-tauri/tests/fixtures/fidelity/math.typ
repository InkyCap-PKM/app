#import "/.inkycap/notebox.typ": *
#note(title: "Mathematics")

= Mathematics

Inline math like $a^2 + b^2 = c^2$ flows within the paragraph.

A displayed equation:

$ integral_0^1 x^2 dif x = 1/3 $

A fraction and a sum together:

$ sum_(k=1)^n k = (n (n + 1)) / 2 $
