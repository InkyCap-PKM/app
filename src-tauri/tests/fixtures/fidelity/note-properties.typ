#import "/.inkycap/notebox.typ": *
#note(
  title: "Quarterly Field Report",
  zid: "20260615T0900",
  tags: ("research", "fieldwork"),
  status: "draft",
  priority: 3,
  pinned: true,
)

= Quarterly Field Report

The body carries ordinary prose so the `#note(...)` property block is
exercised alongside real content. Properties must round-trip through
`typst query` with stable labels.
