#import "/.inkycap/notebox.typ": *
#note(title: "Wikilinks and tags")

= Wikilinks and tags

This note links to #wikilink("Daisy Dog") and to a labelled section via
#wikilink("Field Report", label: "results"). It carries #tag("fieldwork")
and #tag("research") inline.

An external #link("https://typst.app")[reference] closes it out.
