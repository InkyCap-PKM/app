# InkyCap — Claude Code Guidelines

The guidelines themselves live in [`AGENTS.md`](AGENTS.md), the filename the
wider coding-agent ecosystem has settled on. **Edit that file, not this one.**

This stub exists because Claude Code only auto-loads a file named `CLAUDE.md`;
it resolves the `@` import below, so Claude Code sees the same guidelines every
other agent does, with no duplicated copy to drift out of sync.

@AGENTS.md
